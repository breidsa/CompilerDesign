/*
 * import java.util.ArrayList;
 * 
 * class ID {
 * String name;
 * // String type;
 * // String returnType;
 * // ArrayList<String> parameterTypes;
 * // ArrayList<String> fieldTypes;
 * // int size;
 * 
 * public ID(String name) {
 * this.name = name;
 * // this.type = type;
 * // this.returnType = returnType;
 * // this.parameterTypes = parameterTypes;
 * // this.fieldTypes = fieldTypes;
 * // this.size = size;
 * }
 * 
 * }
 * 
 * class Var extends ID {
 * String type;
 * 
 * public Var(String name, String type) {
 * super(name);
 * this.type = type;
 * 
 * }
 * }
 * 
 * class Function extends ID {
 * String returnType;
 * ArrayList<String> parameterTypes;
 * 
 * public Function(String name, String returnType, ArrayList<String>
 * parameterTypes) {
 * super(name);
 * this.returnType = returnType;
 * this.parameterTypes = parameterTypes;
 * }
 * 
 * }
 * 
 * class Struct extends ID {
 * ArrayList<String> fieldTypes;
 * int size;
 * 
 * public Struct(String name, ArrayList<String> fieldTypes, int size) {
 * super(name);
 * this.fieldTypes = fieldTypes;
 * this.size = size;
 * }
 * 
 * }
 */